<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.file.filetest.dao.UserFileDao">
    <select id="selectFile" resultType="com.file.filetest.pojo.MyFIle">
        select * from t_file where uid=#{uid}
    </select>
</mapper>